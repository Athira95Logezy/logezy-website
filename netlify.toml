[build]
  command = "npm run build"
  publish = "dist"

# Redirects are generated at build time by scripts/prerender.mjs into
# dist/_redirects: explicit 200 rules for every real (prerendered) route,
# 301s for legacy /blog URLs, and a final wildcard returning a true 404.
